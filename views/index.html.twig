<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Weekday Finder</title>
  </head>
  <body>
    <form action="/find" method="post">
      <label for="input">Enter the date</label>
      <input id="input" name="input" type="text" placeholder="mm/dd/yyyy or mm-dd-yyyy">
      <button type="submit">Find</button>
    </form>
    {% if result is not empty %}
      <h3>{{ result }}</h3>
    {% endif %}
  </body>
</html>
